import { assertBudget,recordEstimatedCost } from './budget.js';import { blueprintSchema,generatedQuestionSchema,reviewSchema } from './schema.js';import { providers,type ProviderResult } from './providers.js';
const json=(text:string)=>JSON.parse(text.replace(/^```json\s*/,'').replace(/```\s*$/,''));const cost=(r:ProviderResult)=>recordEstimatedCost(r.usage.inputTokens,r.usage.outputTokens);
export async function runPipeline(input:unknown){assertBudget();const blueprint=blueprintSchema.parse(input);const {generator,reviewer}=providers();const provenance:{step:string;provider:string;model:string;cost:number}[]=[];const generated=await generator.generate('Construa uma questão inédita de treino médico. Responda apenas JSON válido, com quatro alternativas ABDC e uma correta. Não alegue revisão humana.',`Blueprint: ${JSON.stringify(blueprint)}. Produza area, topic, stem, alternatives[{id,text,rationale}], correct, pearl, source e source_claim.`);provenance.push({step:'generate',provider:generated.usage.provider,model:generated.usage.model,cost:cost(generated)});const question=generatedQuestionSchema.parse(json(generated.text));const blinded={...question,correct:undefined,pearl:undefined,alternatives:question.alternatives.map(({rationale,...a})=>a)};const reviewed=await reviewer.generate('Resolva e critique sem confiar no gabarito do autor. Responda somente JSON.',`Questão cega: ${JSON.stringify(blinded)}. Produza chosen_answer (A/B/C/D/NONE/AMBIGUOUS), safe, blocking_issues[], quality_issues[], source_needs_check e recommendation (approve/repair/block).`);provenance.push({step:'blind_review',provider:reviewed.usage.provider,model:reviewed.usage.model,cost:cost(reviewed)});const review=reviewSchema.parse(json(reviewed.text));const agrees=review.chosen_answer===question.correct;const status=review.recommendation==='block'||!review.safe?'blocked':agrees&&review.recommendation==='approve'?'auto_verified':'needs_repair';return{question,status,review,provenance,disclaimer:'Verificação automática não equivale a revisão por especialista.'}}
