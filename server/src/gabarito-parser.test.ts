import { describe,expect,it } from 'vitest';import { parseOfficialKey } from './gabarito-parser.js';
describe('parser de gabarito oficial',()=>{it('lê colunas paralelas e estados editoriais',()=>{const text=' 1 A   11 Anulada   21 A\n 2 Excluída* 12 D\n 10 Anulada Administrati- vamente**';expect(parseOfficialKey(text)).toEqual([{number:1,answer:'A',status:'valid'},{number:2,answer:null,status:'excluded'},{number:10,answer:null,status:'administratively_annulled'},{number:11,answer:null,status:'annulled'},{number:12,answer:'D',status:'valid'},{number:21,answer:'A',status:'valid'}])})});
