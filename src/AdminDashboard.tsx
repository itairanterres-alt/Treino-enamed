import { Activity, CircleAlert, CircleDollarSign, ShieldCheck, Users } from 'lucide-react';
import type { ReactNode } from 'react';
import type { Attempt } from './types';
export function AdminDashboard({attempts}:{attempts:Attempt[]}){const correct=attempts.filter(a=>a.correct).length;const highWrong=attempts.filter(a=>!a.correct&&a.confidence===3).length;return <><header><div><p className="eyebrow">ADMINISTRAÇÃO · MODO DEMONSTRATIVO</p><h1>Visão de funcionamento e desempenho</h1><p>Os indicadores atuais usam somente tentativas deste navegador. Dados de uma coorte aparecerão após conexão do banco.</p></div><span className="status experimental">Dados locais</span></header><div className="adminMetrics"><AdminMetric icon={<Users/>} label="Usuários cadastrados" value="1" note="perfil demonstrativo"/><AdminMetric icon={<Activity/>} label="Respostas registradas" value={String(attempts.length)} note={`${correct} corretas`}/><AdminMetric icon={<CircleAlert/>} label="Erros com alta confiança" value={String(highWrong)} note="prioridade formativa"/><AdminMetric icon={<CircleDollarSign/>} label="Custo de IA" value="$ 0,00" note="pipeline ainda desconectado"/></div><section className="panel"><div className="sectionTitle"><div><p className="eyebrow">GOVERNANÇA</p><h2>Permissões previstas</h2></div><ShieldCheck/></div><div className="permissionGrid"><div><b>Estudante</b><span>Apenas os próprios dados</span></div><div><b>Revisor</b><span>Questões, sem desempenho nominal</span></div><div><b>Analista</b><span>Indicadores agregados</span></div><div><b>Administrador</b><span>Usuários, banco e auditoria</span></div></div></section><section className="panel"><h2>Indicadores que serão habilitados</h2><div className="comingGrid"><span>Ativos em 7 e 30 dias</span><span>Retenção por semana</span><span>Desempenho por área</span><span>Confiança × acerto</span><span>Qualidade das questões</span><span>Custo por questão gerada</span></div></section></>}
function AdminMetric({icon,label,value,note}:{icon:ReactNode;label:string;value:string;note:string}){return <div className="adminMetric"><span>{icon}</span><small>{label}</small><b>{value}</b><p>{note}</p></div>}
