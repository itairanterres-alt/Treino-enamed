import { useState } from 'react';
import { ChevronRight, LockKeyhole, Mail } from 'lucide-react';
import { supabase, useAuth } from './auth';
export function Login(){const {signIn,enterDemo}=useAuth();const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const submit=async()=>{setBusy(true);try{setMessage(await signIn(email))}catch(e){setMessage(e instanceof Error?e.message:'Não foi possível entrar.')}finally{setBusy(false)}};return <div className="loginPage"><div className="loginPanel"><div className="brand dark"><span>T</span><div>Treino <b>ENAMED</b><small>acesso protegido</small></div></div><p className="eyebrow">IDENTIFICAÇÃO</p><h1>Seu treino continua de onde parou.</h1><p>Entre por link mágico. Não é necessário criar ou memorizar uma senha.</p><label className="emailField"><Mail size={18}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="seu@email.com"/></label><button className="primary full" disabled={!email||busy} onClick={submit}>{busy?'Enviando…':'Receber link de acesso'} <ChevronRight size={18}/></button>{message&&<div className="loginMessage">{message}</div>}<div className="demoBox"><b>Modo de demonstração</b><p>Use os perfis abaixo enquanto o projeto Supabase não estiver conectado.</p><div><button onClick={()=>enterDemo('student')}>Entrar como estudante</button><button onClick={()=>enterDemo('admin')}>Entrar como administrador</button></div></div><div className="secure"><LockKeyhole size={15}/> {supabase?'Autenticação conectada ao Supabase.':'Supabase ainda não configurado; nenhum e-mail será enviado.'}</div></div></div>}
